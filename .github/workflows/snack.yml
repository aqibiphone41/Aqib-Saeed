name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

# Add this block to grant push permissions
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # ... rest of your code ...
